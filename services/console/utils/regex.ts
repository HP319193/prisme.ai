export const SLUG_MATCH_INVALID_CHARACTERS = new RegExp(
  '[^a-zA-Z0-9 _-]*',
  'g'
);

export const WORKSPACE_SLUG_VALIDATION_REGEXP = /^[a-z0-9-]+$/;
export const SLUG_VALIDATION_REGEXP = /^[a-zA-Z0-9 _-]+$/;
