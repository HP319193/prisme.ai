export const notification = {
  success: jest.fn(),
  error: jest.fn(),
};

export function Spin({ children = null }) {
  return children;
}

export function Card({ children = null }) {
  return children;
}

export function Divider({ children = null }) {
  return children;
}

export function Switch({ children = null }) {
  return children;
}

export function Tooltip({ children = null }) {
  return children;
}

export function Select({ children = null }) {
  return children;
}
Select.Option = function Option({ children = null }) {
  return children;
};
Select.OptGroup = function OptGroup({ children = null }) {
  return children;
};

export function Panel({ children = null }) {
  return children;
}

export function Collapse({ children = null }) {
  return children;
}

export function Input({ children = null }) {
  return children;
}
Input.TextArea = function TextArea({ children = null }) {
  return children;
};

export function Typography({ children = null }) {
  return children;
}

export function Button({ children = null }) {
  return children;
}

export function DatePicker({ children = null }) {
  return children;
}

export function Popconfirm({ children = null }) {
  return children;
}

export function Menu({ children = null }) {
  return children;
}

export function Dropdown({ children = null }) {
  return children;
}
